version: 2.0.0

commands:
  override: true
  record:
    folder:
      '{cfgd}/out/{today:%Y-%m-%d}'
    policy: overwrite
    duration:
    video:
    motion:
    show_video: false
    metadata:

streams:
  override: true
  video:
    world:
      device_type: uvc
      device_uid: Pupil Cam1 ID2
      resolution: (1280, 720)
      fps: 30
      color_format: bgr24
  motion: {}
