version: 1.0.0

commands:
  record:
    folder:
      '{cfgd}/out/{today:%Y-%m-%d}'
    policy: overwrite
    duration:
    video:
    motion:
    show_video: false
    metadata:

streams:
  video:
    world:
      device_type: uvc
      device_uid: Pupil Cam1 ID2
      resolution: (1280, 720)
      fps: 30
      color_format: bgr24
  motion: {}
